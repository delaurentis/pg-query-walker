{
  "name": "pg-query-walker",
  "version": "1.0.0",
  "description": "Analyzes a SQL query you pass in, and provides a list of all database tables and columns that the query references.  Since it uses the PostgresQL query parser, it can process any valid PostgresQL query.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [
    "postgresql",
    "postgres",
    "pg",
    "query",
    "columns",
    "tables",
    "analyzer",
    "dependencies"
  ],
  "author": "Pete DeLaurentis",
  "license": "ISC",
  "dependencies": {
    "pg-query-parser": "^0.2.4"
  }
}
